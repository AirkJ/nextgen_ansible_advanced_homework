---
# tasks file for osp.servers
- name: Create instances
  os_server:
    cloud: openstack
    name: "{{ __inst.0.name }}"
    image: "{{ __inst.0.image }}"
    key_name: "{{ __inst.0.key_name }}"
    flavor: "{{ __inst.0.flavor }}"
    security_groups: "{{ __inst.0.security_group }}"
    delete_fip: yes
    wait: no
    nics:
      - net-name: int_network
    meta: "group={{ __inst.0.meta.group }},deployment_name={{ __inst.0.meta.deployment_name }}"
    userdata: |
      #!/bin/bash
      curl -o /tmp/openstack.pub http://www.opentlc.com/download/ansible_bootcamp/openstack_keys/openstack.pub
      cat /tmp/openstack.pub >> /home/cloud-user/.ssh/authorized_keys
  loop: "{{ lookup('dict', osp_servers, wantlist=True) }}"
  # lookup('dict', single_key_dict, wantlist=True)
  loop_control:
    loop_var: __inst
    

- name: Add floating IP to instance
  os_floating_ip:
    cloud: openstack
    state: present
    reuse: yes
    server: 
    network: ext_network
    wait: true
    timeout: 200
  